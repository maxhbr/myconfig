# Automatically migrated from legacy .beetsconfig.

directory: /mnt/media/music
library: /mnt/media/music/musiclibrary.blb
#path_format: $genre/$artist/$album/$track $title
#plugins: bpd
ignore: 00-Beets 00*

import: 
    move: yes
    quiet_fallback: skip
    log: /mnt/media/music/beetslog.txt

paths:
    singleton: $artist/$artist - $title
    #default: $genre/$albumartist/$album/$track $title
    #comp: $genre/$album/$track $title
    #albumtype:soundtrack: Soundtracks/$album/$track $title

plugins: mpdupdate fromfilename 

mpdupdate:
    host: localhost
    port: 6600
    password: mpd
