name: CI

on: [push]


jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        host: [minimal, base, vserver]

    steps:
    - uses: actions/checkout@v1
    - name: Install Nix
      uses: cachix/install-nix-action@v6
    - run: ./test/test-nixos-configuration.sh --ci ${{ matrix.host }}

