hostname
hostid
*.iso
*.log
/_logs/
/__out/
*.orig
result
*.tar.gz
*.qcow2
.stack-work
*.hi
*.o
.#*
xmonad/dist
_inWork
_misc
result
result-*
.vagrant
myconfig.out-link
*.img
__old_modules/
session-*
__pycache__

# semi-safe-keypair: only commit the public key
misc/semi-safe-keypair

# bd (beads) runtime lock, recreated on every run and left behind when a
# run is interrupted
.beads.gate.lock
